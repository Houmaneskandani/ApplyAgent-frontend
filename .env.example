# REQUIRED. The base URL of your backend API. The frontend will hard-fail at
# the first API call if this is missing — preview deployments must NOT silently
# fall through to production.
VITE_API_URL=https://your-railway-app.up.railway.app
